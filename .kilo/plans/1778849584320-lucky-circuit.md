# SAC.py Refactoring Plan

## Overview

Refactor controllers/SAC/SAC.py (679 lines) to improve modularity, readability, and PEP 8 compliance while preserving all functionality. Changes are grouped into independent phases.

---

## Phase 1 — Extract Shared Utilities to controllers/common/

### 1.1 Extract RunningMeanStd ? controllers/common/running_mean_std.py

**File:** SAC.py lines 180–204
**Action:** Move the class to controllers/common/running_mean_std.py, import back into SAC.py.

**Changes in SAC.py:**
- Delete lines 180–204 (class RunningMeanStd)
- Add rom controllers.common.running_mean_std import RunningMeanStd

**Rationale:** Generic online normalization utility — reusable, and reduces SAC.py by 25 lines.

### 1.2 Extract SequenceReplayBuffer ? controllers/common/sequence_replay_buffer.py

**File:** SAC.py lines 119–177
**Action:** Move the class to controllers/common/sequence_replay_buffer.py, import back.

**Changes in SAC.py:**
- Delete lines 119–177
- Add rom controllers.common.sequence_replay_buffer import SequenceReplayBuffer

**Rationale:** Self-contained sequential replay logic. Reduces SAC.py by ~59 lines, independently unit-testable.

---

## Phase 2 — Fix Code Quality Issues

### 2.1 Move import math to top of file

**File:** SAC.py line 324 (inside _soft_update)
**Action:** Remove inline import math, add import math at the top (line 4, with other stdlib imports).

**Rationale:** PEP 8 violation — imports must be at module level.

### 2.2 Split semicolon multi-statement in _critic_forward

**File:** SAC.py line 347
**Current:** q_val = q_head(torch.cat([features, action], dim=-1)); return q_val, next_state
**Action:** Split into two lines

**Rationale:** Semicolons joining statements on one line are non-idiomatic.

### 2.3 Break up long lines (>100 chars)

**File:** SAC.py lines 241–244, 245–249, 332, 390, 407

| Lines | Issue |
|-------|-------|
| 241–244 | actor_optimizer — combined list() calls exceed 120 chars |
| 245–249 | critic_optimizer — same pattern |
| 332 | _sequence_loss_mask return (~155 chars) |
| 390 | critic_params = list(...) + list(...) + ... (~140 chars) |
| 407 | actor_params = list(...) + list(...) + ... (~130 chars) |

---

## Phase 3 — Refactor SACAgent Class

### 3.1 Extract network factory methods from __init__

**File:** SAC.py lines 216–239
**Action:** Lift nested _make_encoder() and _make_critic() closures into class-level private methods:
- _build_actor_encoder() ? returns (actor_enc, actor_mean, actor_log_std_head)
- _build_critic_pair() ? returns (q_enc, q_head, target_enc, target_head)

### 3.2 Extract _obs_rms_tensors() helper

**File:** SAC.py lines 364–367
**Action:** Extract obs normalization into a helper.

### 3.3 Extract parameter list properties

**File:** SAC.py lines 390, 407
**Action:** Replace inline list(...) with @property helpers _critic_params and _actor_params.

### 3.4 Break update() into focused sub-methods (87 lines ? 6 helpers)

**File:** SAC.py lines 349–435
**Action:** Extract:
- _prepare_done_masks(batch) (lines 354–361)
- _compute_target_q(batch, done_mask_next) (lines 369–379)
- _update_critic(batch, target_q, learn_mask, done_mask_obs) (lines 381–393)
- _update_actor(batch, learn_mask, done_mask_obs) (lines 395–410)
- _update_alpha(log_prob, learn_mask) (lines 401–403, 412–415)
- _update_target_networks() (lines 417–421)

---

## Phase 4 — Refactor 	rain() Function

### 4.1 Extract LR warmup helper (lines 581–587)
### 4.2 Extract checkpoint helpers (lines 613–633)
### 4.3 Extract rolling stats and log-line formatting (lines 635–643)

---

## Phase 5 — File Summary

| File | Action |
|------|--------|
| controllers/common/running_mean_std.py | **New** |
| controllers/common/sequence_replay_buffer.py | **New** |
| controllers/SAC/SAC.py | **Edit** |

---

## Verification

1. No behavior change — training loop, forward pass, and update math remain identical
2. All imports resolve correctly
3. No new PEP 8 violations (verify with ruff or pycodestyle)

